// src/World/Shaders/cloudDepthFragment.glsl
// Version de test - Ne fait rien, laisse la profondeur par défaut s'écrire

void main() {
    // Pas de discard, pas de calcul, pas d'écriture explicite.
    // La profondeur interpolée devrait être écrite par défaut.
}